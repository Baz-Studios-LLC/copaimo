---
name: neon-drift-project
description: "NEON EDGE (formerly NEON DRIFT) — neon vector-Asteroids love letter; modular ES-module game at Desktop/neon-asteroids, played via Artifact bundle"
metadata: 
  node_type: memory
  type: project
  originSessionId: e087e0de-1f68-4dbf-93b4-93dd3a17b6e9
---

**NEON EDGE** (renamed from **NEON DRIFT** on 2026-07-14 — the old name collided with Steam's NEON DRIFT ULTRA + Neon Drive; "Neon Edge" only brushes a tiny itch.io jam entry) is a neon "love letter to Asteroids" (Geometry-Wars glow): neon-blue wireframe asteroids of varied sizes, a neon-purple triangle ship, heavy bloom. Started 2026-07-09. `config.js` `TITLE = "NEON EDGE"`.

- **Location:** `C:\Users\jsull\Desktop\neon-asteroids\` — modular ES modules under `js/` (config, utils, audio, input, render, arena, particle, bullet, chain, powerup, asteroid, mine, enemy, boss, ship, game, main), `css/style.css`, entry `index.html`. Modular by request (no monoliths). `bundle.py` `MODULE_ORDER` must list every module (powerup.js sits after chain.js).
- **Wave schedule (time-based, config PHASE/MINE/DENSE/ENEMY/ORANGE):** ACT 1 (W1-10) — W1 asteroids (BLUE), W2 +mines, W4 +enemy mobs, W5 = octopus boss, 10s calm, W6 = all hazards + chain-shot pickup window. **Asteroids flip fully GREEN (dense) from W6** (`DENSE.firstPhase=6`, `chance=1.0`). W6-9 normal green loop; W10 = red devourer boss + mass-shot pickup window. **ACT 2 (W11+) is being hand-authored by the user for a deliberate difficulty ramp:** **mines & mobs STOP after W10** (`MINE.lastWave`/`ENEMY.lastWave` = 10 → `mineTarget`/`enemyTarget` return 0 for `level>10`) — W11 is green-asteroids-ONLY (a clean breather to enjoy the mass shot). **W12+ introduces ORANGE volatile asteroids** (`ORANGE.firstWave=12`, ~30% of spawns). More waves TBD (user directs). Mines/mobs are ALSO suppressed during any boss wave (`this.bossWave`). Chain shot doesn't gate spawns. Boss cameo + WARNING telegraph in the last ~5s of the pre-boss wave (W4/9/14). Audio = thruster hum + shot + break + boss warning (Web Audio, gated on first gesture). **If the user wants mines/mobs back later, bump `lastWave` or add per-wave logic.**
- **Playable build:** `build/bundle.py` concatenates modules → self-contained `dist/neon-edge.html` (renamed from `neon-drift.html` on 2026-07-14), published as an Artifact. See [[neon-drift-keep-playable]] — rebuild + redeploy after every change.
- **GitHub (added 2026-07-10):** PRIVATE repo **`Baz-Studios-LLC/Neon-Drift`** (https://github.com/Baz-Studios-LLC/Neon-Drift), `origin`/`main`, README + .gitignore committed (dist/ IS committed). **PENDING (2026-07-14): repo should be renamed `Neon-Drift` → `Neon-Edge` to match the game.** Couldn't do it from here (no `gh` CLI + the earlier token was revoked); the USER must rename it on GitHub (Settings → Rename). GitHub auto-redirects the old URL so `git push` keeps working; after the rename, update the local remote: `git remote set-url origin https://github.com/Baz-Studios-LLC/Neon-Edge.git`. The org slug is `Baz-Studios-LLC` (NOT "Bazstudios"). Auth: no `gh` CLI installed; push works via Git Credential Manager (interactive browser popup). Commits author as the machine's global git identity `Phantom-Jay <cybersulli89@gmail.com>` (personal, not the bazstudios email).
- **Desktop releases (Tauri v2, added 2026-07-10):** Mac + Windows apps via **Tauri** (system webview → tiny installers; user picked Tauri over Electron). Rust shell in `src-tauri/` (`Cargo.toml`, `tauri.conf.json` frontendDist=`../dist`, `src/main.rs`, `capabilities/default.json`, `csp:null` so the inline game runs). Frontend = `dist/index.html` (full standalone doc that `build/bundle.py` now emits alongside the Artifact fragment). Branded neon icon: `src-tauri/app-icon.png` → `tauri icon` → `src-tauri/icons/`. **CI** = `.github/workflows/release.yml`: matrix `windows-latest` + `macos-latest` (universal mac), triggered on push to `main` (rolling **`latest`** prerelease) and `v*` tags (versioned Release); a `prepare` job pre-creates the release so the matrix uploads via `releaseId` (no race/stale assets). Builds are **UNSIGNED** (SmartScreen/Gatekeeper warn; add Windows cert + Apple Developer ID later). **CAN'T build/verify locally** — no Rust here + Mac needs macOS, and no `gh` to watch runs → first CI run may need a fix (verify by checking the repo's Actions/Releases or have user report). Keep releases in sync by pushing to main (see [[neon-drift-keep-playable]]).
- **Local dev:** served by the `neon-asteroids` launch config (python http.server on 8188) at `/neon-asteroids/index.html`.
- **Boundary model (current):** NO border, NO walls, NO wrap. Full-screen grid; screen edge is the bound. Ship soft-clamps at the edge (stops, no bounce); asteroids that drift off are removed and new ones stream in from the edges (recycle keeps population = `PHASE.populationBase + level`); bullets are removed at the edge. Arena is full viewport (createArena → 0..W/0..H). `arena.js` helpers: `clampToArena`, `isInside`, `isOffscreen`. (History: was walls→bounce, then wrap+ripple border — both removed.)
- **Phases (TIME-based, seamless):** advance by surviving a timer — `PHASE.secondsBase + secondsPerLevel*(level-1)` (L1 55s, L2 65s…). No kill-count/quota (user rejected the number target). Field never resets on advance; the new phase's hazards drift IN from the edges. HUD = `PHASE n` + a thin top progress bar (`drawTimeBar`); "PHASE n" banner flashes on advance. Population/speed still scale with level.
- **Dense asteroids (`DENSE` config, phase 3+):** neon-green tougher rocks that drift in from edges (fraction `DENSE.chance`). Have HP = size (large takes 3 hits), heavier `massMult`, slower, worth `scoreMult`×; split into dense children. `createAsteroid(...,type)` + `hitAsteroid()` (chips HP, breaks at 0). Asteroids carry `type`/`color`/`hp`/`pulse`.
- **Orange volatile asteroids (`ORANGE` config, `type:"orange"`, wave 12+):** neon-orange rocks (`#ff7b1a`). **WIREFRAME only — no fill:** the OUTLINE pulses (breathes brighter+thicker via `a.pulse`) to read as "about to blow" — an earlier filled core was removed (user: asteroids never have a core, see [[neon-drift-asteroids-core]]). `hp:1` (pop in ONE hit). On destroy they DON'T split — `destroyAsteroid` routes them to `explodeOrange(x,y)`: a BIG AoE (`blastRadius` 200, checked against each target's radius so anything TOUCHING it goes — user said the old 122 range was too small / "didn't destroy nearby") that VAPORIZES every asteroid in range (awarding score), CHAINS to any other orange caught (recursive — each removed before its blast, so it terminates), spawns a juicy DOUBLE shockwave (white hot core + orange front — `particle.js` `shockwave()` = `ring:true` particles grown/drawn by updateParticles/drawParticles) with heavy debris + yellow-hot sparks + a strong shake (user wanted more effect), and KILLS the ship if inside the blast (also `+ship.r`). Double-edged crowd-clearer — great from range, lethal up close; left for players to discover ([[favor-emergent-skill-discovery]]). ~30% of edge-spawns from W12 (`ORANGE.chance`).
- **Other mechanics:** asteroids collide elastically and only break when shot; bullets have a neon glow + short trail; fire = SPACE or left-click; H = hyperspace.
- **Aim assist (config `AIM`, `game.assistedAim`, added 2026-07-14):** user found small/distant targets + bosses hard to hit (keyboard rotate-to-aim; `SHIP.turnRate` 0.05 is too coarse to point exactly at range). On `fire()` it finds the hostile (entered asteroids / enemies / boss) MOST aligned with the ship's facing within `AIM.cone` (~11°) and `AIM.range` (900), and bends the shot's launch angle toward it by up to `AIM.maxBend` (~7°). So within ~7° → dead-on, 7–11° → partial, off-cone / no target → straight (still real manual aim, NOT auto-aim). Chose this over mouse-aim (would change the control scheme), hitbox padding (phantom hits), and a slow-aim key. Tune/disable via `AIM` (cone=0 disables). Only the primary gun (`fire`, incl. mass shot) — chain is AoE, drone already auto-aims.
- **Warp missile / black hole (`warp.js`, config `WARP`, key `B`, added 2026-07-10):** a core ability available FROM THE START (not a pickup) — 3 charges on a long recharge (`rechargeFrames` 900). Fires a slow missile (`createWarpMissile`) that after `missileLife` frames tears open a small BLACK HOLE (`createBlackHole`). For `holeLife` (~2.5s) `blackHolePull` drags every hazard within `pullRadius` (360 = "the quadrant") toward the centre (position-based lerp, accelerating + `swirl`) and CONSUMES what reaches `consumeR`: asteroids/mines/mobs/enemy-bullets, awarding their score. **The SHIP is NEVER pulled** (pull only iterates hazard lists), and bosses + their shield are immune too. drawBlackHoles = dark event-horizon core (rim FIRST then non-glowing dark fill on top so centre stays dark) + cyan accretion arcs + violet halo + faint pull-zone ring. Bottom-LEFT `drawWarpHud` charge pips (chain owns bottom-centre). On-pillar: it's a limited-charge tool that ACTS ON asteroids (crowd control), doesn't overshadow them.
- **Terminology:** user-facing waves are called **"WAVE n"** (not "phase") in the HUD/banner; internal code still says phase (phaseTimer, advancePhase). Every 5th wave is a BOSS wave.
- **Mines (`mine.js`, config `MINE`):** wave-2+ hazard. Neon-crimson proximity mines that arm+blink when the ship is within `triggerRadius`, then detonate after `fuse` frames (kill within `blastRadius`); also detonate on contact; shootable for score. They now FLOW: drift at `MINE.speed` across the field, recycle off the far edge, `topUpMines()` streams new ones in (count capped as fraction of asteroids). **Mine blasts break asteroids** in `blastRadius` (one hit each) and fling the chunks at `MINE.chunkSpeed`× — an asteroid-management tool (`blastAsteroids`, `mineTouchesAsteroid`).
- **Enemies / mobs (`enemy.js`, config `ENEMY`):** wave-4+ neon-yellow orbs that hover-strafe and fire slow aimed shots. **Die in ONE shot** (`ENEMY.hp = 1`; user wanted them fragile). **They can't stack** — `game.separateEnemies()` (called each frame from `updateEnemies`) pushes any overlapping pair apart + nudges their velocities so they spread around the ship instead of piling on one spot. Count capped as a fraction of asteroids; suppressed during boss waves.
- **Boss waves (`boss.js`, every 5th wave) — THREE types, cycling by boss number (2026-07-14):** `advancePhase` sets `this.bossType = this.bossTypeForNumber(level/waveInterval)` where `bossTypeForNumber(n) = ["octopus","red","ship"][(n-1)%3]` → OCTOPUS on 5/20/35…, RED DEVOURER on 10/25…, RAIDER SHIP on 15/30…. Companion helper `bossColorForType(t)` is the single source for the boss tint (schedule, pre-boss cameo, death burst). `bossType` is read only by `updateBossIntro` (which creates the right boss). The pre-boss cameo/WARNING (last ~5s of W4/9/14) is tinted to the *upcoming* boss's colour and says "DEVOURER INCOMING" for red. Shared flow for both: intro (mines/enemies flee via `setFlee`/`updateBossIntro`, asteroids stay) → enter → CHARGE (`BOSS.chargeFrames`, invulnerable, `drawBossBar` fills) → fight → DEATH (`startBossDeath`/`updateBossDeath`/`finishBossDeath`, per-type colour) → 10s intermission → advance. Boss waves end by kill, not timer; mines/enemies suppressed. Dispatch points in `game.js` branch on `boss.type`: step (`stepBoss`/`stepRedBoss`/`stepRaider`), `bulletCollisions` boss block (octopus has a shield; red + ship take direct body hits — they SHARE that branch), and render (`drawBoss`/`drawRedBoss`/`drawRaider`). Red + raider both carry an empty `shield:[]` so shared death/finish code is safe.
  - **OCTOPUS (`createBoss`/`updateBoss`/`stepBoss`/`throwRock`, config `BOSS`):** neon-magenta core with asteroids grabbed onto long undulating tentacle arms (`boss.shield` slots at `orbitRadius`); a captured rock REELS IN to its slot over `BOSS.grabFrames` (22, exponential ease k=0.18 in `updateBoss`) then hard-pins — was an instant SNAP, user flagged it 2026-07-14 (capture sets `a.grab=0`; the shrink-in-place replacement sets `grab=grabFrames` so it doesn't re-reel); shield rocks block shots, only bullets through a gap hit the core (`hp` 28); contact kills. **Boss-1 idea (user):** it REACHES for the LARGEST rock in range (capture picks max `size`, tiebreak nearest — big rocks = better shield) and THROWS held rocks at the ship — `throwRock()` hurls only a SIZE-1 rock (smallest tier) as a fast asteroid (`BOSS.throwSpeed` 4.6) re-added to `this.asteroids` (dodge or shoot it; lethal on contact like any rock) — it keeps size 2/3 as shield and HOLDS FIRE until it has a size-1 (user: "only toss the smallest size ones"). Size-1s appear as the player shrinks shield rocks, so throwing is a consequence of attacking the shield. Throw cadence = `fireEvery`/`fireJitter` (REPLACED the old energy shot — the boss no longer emits `createEnemyBullet`). Shooting a shield rock now SHRINKS it in-slot (`breakShieldRock`: size→size-1 same slot; size-1 shatters + frees slot) → persistent shield AND it feeds the octopus small fragments to throw. Thrown rocks get `a.thrown = 90`: a magenta danger halo (`drawAsteroid`) + skipped by capture so it isn't instantly re-grabbed (`updateAsteroid` decrements it). Top-up CONTINUES (needs shield material). **Visual:** `drawBoss` = jagged magenta star + soft core fill only — the on-boss white HP ring was REMOVED (user, like the devourer); HP shows on `drawBossBar` only.
  - **RED DEVOURER (`createRedBoss`/`updateRedBoss`/`drawRedBoss`/`stepRedBoss`, config `RED`, wave 10+):** big jagged RED asteroid that does NOT split — bullets hit it directly (no shield) and drain `hp` (`hpStart` 30, `hpPerAbsorb` 2, capped at `hpCap` 60 — HP stays killable; SIZE is the real threat). **No projectiles** (an earlier spit-fan + lunge were removed — user: threat from feeding, not projectiles). It CHASES the NEAREST asteroid to devour it (`stepRedBoss` picks the closest rock → `updateRedBoss(boss, target, arena)` charges it at constant `RED.speed` 1.3 — NO size-scaling; user said the old bigger=faster was too aggressive); field EMPTY ⇒ it hunts the ship. Devours on contact within `absorbReach` (22): +`growPerAbsorb` 6 size (was 9), +HP to the cap. **SIZE-OUT mechanic (user):** NO fixed max size — it BLOATS steadily over time (`growPerFrame` 0.03/frame ≈ the "clock"; growPerAbsorb+growPerFrame both cut ~half on 2026-07-14 — user: swelling too fast to kill in time) on top of eating, capped at `min(arena.w,arena.h) * maxRFraction` (0.52). When it hits that cap `stepRedBoss` calls **`devourerSizedOut()`**: it OVERLOADS — a supernova (double shockwave + heavy debris + big shake) — then **costs ONE life (`this.loseLife()`) and the battle RESTARTS: the devourer reforms at its original size + HP** (`b.r=startR; b.hp=b.maxHp=hpStart; b.charging=true`). It is NOT an instant run-ender (user reconsidered — unfair at 3 lives); you only lose the run if it takes your LAST life (via `loseLife`'s normal game-over). Repeatable — it can re-grow and reset again. DEV `invincible` needs no special case: `loseLife` no-ops, so the boss just resets and testing continues. (Earlier design was a hard `lives=0` game-over + a `sizedOut` flag/advance-guard — all removed.) Deny food + kill fast to avoid it. Clamp centres the blob once it's arena-sized. **Top-up stays ON** during the wave (rocks keep streaming to chase/deny). Frozen+invulnerable during charge. Chain beam does NOT hit its core (only clears food rocks). **Visual:** plain red asteroid outline — no core fill / HP ring (see [[neon-drift-asteroids-core]]); HP on `drawBossBar`.
  - **RAIDER SHIP (`createRaider`/`updateRaider`/`drawRaider`/`stepRaider`, config `RAIDER`, wave 15+, added 2026-07-14):** a hostile-teal (`#2ce8cf`) enemy SHIP (arrowhead hull + claw prongs) that hovers at range using the SHARED `hoverStrafe(o,ship,arena,cfg)` steering extracted from `enemy.js` (same as the yellow mobs — DRY). State machine `seek → tractor → aim → cooldown` (tractor added 2026-07-14 — user: "still too fast, want a slow tractor-beam grab"): in `seek` it locks onto the nearest on-screen field rock (`entered && !thrown`), splices it OUT of `this.asteroids` into `boss.held`, and enters `tractor` for `RAIDER.pullFrames` (90 ≈ 1.5s) — updateRaider SLOWLY reels the rock from its field spot to the nose (ease 0.05, not the instant pin), and drawRaider renders a teal TRACTOR BEAM (halo + white core) from nose to rock; then `aim` (`aimFrames` 30, short wind-up, prongs flare white) it FIRES — the rock is pushed BACK into `this.asteroids` with `thrown=90` + velocity `shotSpeed` 6.5 toward the ship, so it re-enters the normal asteroid system and every physic works (collides, shootable, kills on contact). `r` 34 (bigger/easier to hit — was 26), `hp` 34, no shield (direct body hits), `cooldownFrames` 80 between throws (doubled — slower grab-and-shoot); contact kills; invulnerable while charging; `startBossDeath` drops any held rock. Verified end-to-end via the reconstruction harness.
- **Power-ups (`powerup.js` = the pickup orb; per-weapon config/logic):** DESIGN PRINCIPLE (user) — **each boss's mechanic directly influences the NEXT power-up** (the power-up counters the boss you just beat). Generic single-slot system: `this.pickup = {kind, x,y,vx,vy,r,rot,pulse}` (null when none); `createPickup(arena,kind)`, `updatePickup` (drift + bounce off ALL edges so it stays on screen), `drawPickup` (kind-branched glyph: chain=lightning+2 dots, mass=fat bullet+impact ticks, drone=mini arrowhead+core), `pickupColor`/`pickupLabel`. Game owns unlock state + spawn/collect (`tickPickup`, `collectPickup`).
  - **Lifecycle:** a power-up spawns in the clear 10s post-boss calm (`finishBossDeath`), keyed to WHICH boss just died — `bossNum = round(level / BOSS.waveInterval)`: boss #1 (wave 5) → chain, boss #2 (wave 10) → mass, boss #3 (wave 15) → drone. **Keyed on boss number, NOT a wave range** — an earlier `level+1 >= unlockWave` range check could re-offer chain at wave 10 if the wave-5 boss was skipped via the dev button (so `chainOffered` stayed false); user caught the chain pickup appearing at wave 10. Offered ONCE per run (`chainOffered`/`massOffered`/`droneOffered`). It stays on screen the whole window; grab by flying through OR shooting it; if the next wave starts first it's cleared for good (`advancePhase` nulls leftover `this.pickup` → "… LOST"). Does NOT gate spawns.
  - **CHAIN shot** (`chain.js`, config `CHAIN`, counters the OCTOPUS's crowds/shield): two bullets perpendicular to aim joined by a PURPLE lightning beam that mows a line (crowd clear). Input RIGHT-CLICK / X (`input.special`); 3 charges on a recharge; purple pip meter bottom-center.
  - **MASS shot** (config `MASS`, counters the DEVOURER, wave 11+): PERMANENT primary-fire upgrade (no ammo/HUD) — standard shots become fatter (`MASS.r` 6 vs `BULLET.r` 2.5 → bigger projectile + hitbox) and stronger (`MASS.damage` 2). Bullets carry `dmg` (default 1); `createBullet(...,r,dmg)`; `hitAsteroid`/`hitEnemy`/`breakShieldRock` take a `dmg` arg and boss-core hits do `hp -= b.dmg`. Clears the field faster (starve the devourer) AND out-damages its HP. **Look:** the fired rounds are a FIERY neon-PURPLE flame, not amber — `drawBullets` branches on `b.dmg > 1` to `drawFieryHead` (flickering purple star licks off `b.life`, soft bloom, white-hot core) + a wider/hotter trail. `MASS.color` amber `#ff9f1c` is only the PICKUP orb, not the bullets (user wanted the shots to stay neon purple).
  - **DRONE / wingman** (`drone.js`, config `DRONE`, counters the RAIDER, waves 16+, added 2026-07-14): PERMANENT wingman for the run (`this.hasDrone`/`this.drone`). A small **purple CIRCLE/ring** (`DRONE.color = COLORS.purple` `#b64bff` — player family, see [[neon-edge-purple-is-player]]; drawn as a ring+core, NOT a triangle, so there's no facing to manage) that hover-follows the ship (an anchor point orbits the ship via `DRONE.orbitSpeed`/`followDist`, drone eases toward it) and AUTO-FIRES at the **nearest on-screen asteroid (NO range limit** — user reported it "doesn't fire"; the old `DRONE.range` 520 cap starved it of targets on big screens / sparse fields, so it was removed) on a slightly slower cadence than the player (`fireCooldown` 18 vs SHIP 12). Idles only when the field is truly empty (the post-boss calm). Split like the ship: `drone.js` does `createDrone`/`moveDrone`/`drawDrone` (movement + draw); `game.updateDrone()` does targeting + firing (it owns the lists). **GOTCHA (fixed 2026-07-14):** `createDrone` does NOT set an instance `r`; `updateDrone` must use `DRONE.r` (config) for the muzzle offset — an earlier `d.r` made the muzzle `NaN`, so bullets spawned at `NaN` (created but invisible + non-colliding) → "drone doesn't fire anything" for two rounds. `drawDrone` also uses `DRONE.r`, so the drone rendered fine, masking it. LESSON: when verifying projectile spawns, assert the POSITION is finite, not just the velocity. Shots are NORMAL bullets (`createBullet(...,DRONE.damage=1)` into `this.bullets`) so existing collision/render handle them — **targets ASTEROIDS ONLY** (reinforces the core loop; keeps boss fights player-driven) and is **SILENT** (no per-shot SFX, else it'd spam `playShoot`). Drone is invulnerable (not a hazard, no collisions); persists across respawns; only drawn while `this.ship` exists. Thematic counter: the raider was a hostile SHIP out-gunning you → your own helper ship.
- **GOTCHA — asteroid seek is 0:** an earlier anti-camp "seek toward ship" (`ASTEROID.seek`) curved asteroids back from the edges (when wrap was the model) so the field read as invisible walls (user reported "still have the walls" twice). Disabled (`seek: 0`); code path kept for a future homing enemy.
- **Roadmap:** DONE — dense rocks, enemies, W5 octopus boss, all-green from W6, W10 RED DEVOURER boss, W15 RAIDER SHIP boss. Boss cycle repeats every 5 waves (octopus → devourer → raider: 5/10/15, 20/25/30, …). Possible next: bonus room on some multiples of 5, more boss variety, per-wave tuning table.
- **Pause / settings menu (Esc, `game.js`, canvas-drawn, mouse+keyboard as of 2026-07-14):** one `pauseRows()` definition per view (menu = RESUME/SETTINGS/MAIN MENU; settings = SOUND ON/OFF toggle + BACK) is the SINGLE source driving the draw, keyboard nav (`pauseNav`/`pauseActivate`), AND mouse hit-test (`pauseHitTest`/`onPointer`) — a click and an Enter run identical code (DRY). `input.js` `installInput` gained an `onPointer(type,x,y)` callback; a consumed (modal) click doesn't also fire the weapon. Settings is JUST an audio toggle for now (user's call); `setMasterVolume` gates the master gain that ALL sfx+music route through. "Main Menu" → `startMenu()`.
- **DIRECTION (user, 2026-07-10):** the game should get **increasingly difficult** as waves climb, via a HAND-AUTHORED per-wave schedule (not just the flat `populationBase + level` / speed `×(1+level*0.05)` scaling). NOW IN PROGRESS — user is directing wave-by-wave from W11: W11 = green-only breather, W12 = +orange volatiles. Expect more per-wave asks (new asteroid types, when to bring mines/mobs back, harder mixes). Boss-1 idea already delivered (octopus throw/reach). When they name a wave, edit the schedule points (`MINE/ENEMY.lastWave`, `ORANGE.firstWave`, `DENSE`, boss parity) rather than the flat scale.
- **DIFFICULTY CAPS (user, 2026-07-14 — "difficult but not impossible"):** the flat ramps are now BOUNDED so late waves stay survivable instead of saturating the no-wrap screen. `PHASE.populationCap` (16) caps `populationTarget` = `min(populationBase+level, cap)` → count ramps to ~W12 then PLATEAUS (ACT 1 W≤10 unchanged, never hits it). `PHASE.speedRampMax` (2.0) caps the per-level speed multiplier `min(1+level*0.05, max)` → asteroid speed plateaus ~W20. **`populationCap` is the PRIMARY hard-but-fair dial** — lower to ease, raise to bite (analogous to easing at the SOURCE/count, not by nerfing the ship). Speed formula is now the single exported `asteroidSpeed(size, level)` in `asteroid.js` (was copy-pasted in `driftVelocity` + `game.spawnFromEdge`). Secondary hazards (`mineTarget`/`enemyTarget`) key off `populationTarget()` so they scale with the capped count automatically.
- **PRE-LAUNCH TODO — remove the dev buttons:** the `DEBUG`-gated block in main.js (marked DEV ONLY) now has TWO buttons — "Skip phase >>" (`game.advancePhase`) and "Invincible: OFF/ON" (toggles `game.invincible`, which makes `loseLife()` a no-op). `game.invincible` defaults false and is never reset. Set `DEBUG=false` (config.js) or delete the whole block before launch. User explicitly wants them gone for release.
- **Verify quirk:** the preview tab is often hidden → `requestAnimationFrame` is suspended, so screenshots freeze and the canvas reads blank. Verify by driving a `Game` instance manually via `preview_eval` and sampling canvas pixels / reading state instead.
- **GOTCHA — the bundle MASKS missing `export`s; the dev page does not:** `bundle.py` strips every `import`/`export` and flattens modules into one IIFE scope, so a symbol imported across files works in the bundle (and in the reconstructed-module harness) even if its declaration is missing the `export` keyword. The real ES-module dev page (`index.html`) will then fail to boot with `does not provide an export named X`. Found the hard way: a botched find/replace had left `const ASTEROID` (not exported, imported by game.js/asteroid.js) and `export export const ENEMY` — both fixed. So when verifying, ALSO load the dev page (or `import()` a module fresh in `preview_eval` and catch the error), not just the bundle/harness. Browser also caches module scripts hard — a stale `main.js` can render old dev buttons even after `location.reload(true)`; fetch the served file with `?ts=` to confirm what's actually on disk.
- **GOTCHA — loop must survive hidden tabs:** the in-app preview pane is frequently `document.hidden`, which fully SUSPENDS `requestAnimationFrame` → a pure-rAF loop freezes and the game "won't start" (reported by user). Fixed in `main.js` with a hybrid loop: rAF for smooth 60fps when visible + a `setInterval` fallback that pumps `frame()` when rAF has stalled (>80ms), plus a `visibilitychange` resync. Don't revert to pure rAF. Also: keep audio init non-blocking (`initAudio` is try/caught) so it can never gate `game.start()`.
