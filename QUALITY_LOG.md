# Quality log

Findings from the quality-control passes, whether or not they were fixed. One
entry per fault, newest first. A fault that is logged and not fixed is not a
failure — the standing instruction is to log and move on rather than spend hours
on one thing.

**Dispositions:** `fixed` · `logged` (real, not done yet) · `by design` (looked
wrong, is not) · `watching` (cannot reproduce reliably yet).

| # | Area | Fault | Disposition |
|---|---|---|---|
| 3 | Roads — village lanes | **The biggest "generated" tell in the game.** A dirt lane is 4.1 m of mesh with a 5.4 m feathered skirt each side, so it reads ~15 m wide; a village's ring-and-radial network lays enough of them over each other that the whole place is one orange disc with houses on it. Measured: the skirt feathers **8.3 cm of height over 5.26 m** — one in sixty-three, invisible as geometry, doing all its work as a colour wash. Cities already close their skirt to 0.35 m when the kerb arrives. Photographs: `qc_village_air.png` (before) vs `qc_village_air2.png` (a 1.5 m skirt) — the second is a village green with lanes rather than a dirt disc. | `logged` — see below |
| 3a | ↳ why it is not a one-line change | Setting the constant to 1.5 fails three guards, and the last one is the real reason: for an unpaved road `lift` returns early, because with no kerb the whole profile IS the skirt. Shortening it therefore does not sharpen an edge, it **domes the lane** — the same 10.5 cm crown over 1.5 m is a 7% camber. The crown has to shrink with the skirt. Groundwork done: the camber span and the skirt span are now separate fields (`camber` / `shoulder`), which was previously one number, so the next attempt can move one without the other. | `logged` |
| 2 | Junctions | A node painted its whole skirt in the FOOTWAY's colour (`4 | 5 => flag`) and then cut straight to grass at the edge, where the ribbon beside it blends to the ground. Invisible while every road wore a five-metre wash of the same colour. | `fixed` |
| 1 | Yards — store yard | The timber stack clips straight through the yard's own fence, and the fence has gaps between panels. `qc_storeyard.png`. Contents are placed by `dev/art/yard.py` `store()` with no clearance against `_rail_fence`. | `logged` |
| 6 | Props — garden | A green cube (hedge/planter) floats in mid-air beside a fence post, well clear of the ground. `qc_bench.png`, right of the warden. | `logged` |
| 5 | Yards — garden | The beds are perfectly mirrored, four a side about a central path. Symmetry like this is the strongest "generated" tell in the art: no gardener plants two identical halves. Same family as the mirrored barrels in `store()` (`for side in (-1.0, 1.0)`). Wants per-bed variation in length, spacing and a broken axis. | `logged` |
| 4 | Furniture scale | Table top stood at 0.85 m (ordinary 0.72-0.76) and the civic bench seat at 0.515 m (ordinary 0.43-0.48). Beside a 1.70 m warden that reads as furniture built for somebody taller - and it is invisible until a person stands next to it, which is why arithmetic found it and photographs had not. Found by Codex's dimensional audit. Table now 0.75 m, bench 0.45 m, legs and back brought down with them. | `fixed` |
| 8 | Yards — store | Two barrels stood at exactly opposite offsets - the same mirror the garden had. Offset, resized and turned a little each. | `fixed` |
| 7 | Props — garden fruit tree | It was a 1.7 m CUBE of foliage on a thin trunk, which photographs as a green crate hovering over the beds while every tree in the wood behind it is a faceted clump. `masonry.lump` is now the one rounded-mass primitive - `trees.py` delegates to it, verified byte-identical on all five species - and the fruit tree is three masses on a trunk. | `fixed` |
| 6 | Yards — garden | Beds were `for side in (-1,1)` by `for row in range(2)`: four identical rectangles with three identical drills, perfectly mirrored about the path. Each bed now has its own size, offset and drill count from `masonry.wobble`, so the mirror is broken by the beds DIFFERING rather than by scattering them - it is still a kitchen garden in rows. | `fixed` |
| 11 | Interiors — stairwell | `room()` laid a full-extent slab at every storey, so the flight ran up into the underside of the floor above and went nowhere. The floors above the ground one are now laid in strips around one shared `stair_well` rectangle, so the flight and the hole cannot disagree. Photographed from the storey above: the flight is visible through the opening. Collision is unaffected - `Floor` records only the ground floor's boards, so upper storeys were never stood on. | `fixed` |
| 10 | Interiors — handrail | An open 3.6 m rise had no handrail at all. Newel at each end and a rail following the flight's pitch. | `fixed` |
| 9 | Interiors — stairs | `rise = STOREY / 10` = 0.36 m a step against a comfortable 0.15-0.22, which with a 0.28 m going is a **52-degree pitch** - a ladder with treads. Found by Codex's dimensional audit; nothing in the room disagrees with it until somebody tries to climb it. Now 18 steps at 0.20 m, a 36-degree flight, whose 5.04 m run fits a cottage's 7.06 m of inner depth. | `fixed` |
| 13 | Roads — gateway cliff | The skirt closed with `outer_tie` (the soft country verge, paved 0.30-0.75) while the KERB that replaces it arrives over 0.62-0.72. In between sat a road with its skirt shut and no kerb yet to end at, so the surface fell to the hem across whatever was left - at a third of a metre that is a 20% slope. Invisible while every other skirt was long enough to hide it. Now closes with `kerb_stands`: a thing that exists because of a kerb asks about the kerb. Same shape as AQ-024. | `fixed` |
| 12 | Roads — unpaved profile | An unpaved road returned early from `lift` and used one parabola all the way out to the CAMBER span, while its mesh stops at the shorter SKIRT - so the surface ended in the air. It now takes the same piecewise path a paved street always has (a kerb of nought collapses the middle branches), which also means the skirt ramps linearly and a chord across it sags not at all. | `fixed` |
| 3b | ↳ village lane, second attempt | Narrowing the skirt to 1.5 m still fails, and the remaining blocker is different from the first one: with the camber decoupled the carriageway is fine, but a colour-only version - keeping the 5.4 m geometry and adding a station partway down the skirt so the ground's colour arrives sooner - produces 711 down-facing triangles in a village. The ribbon's band-holding logic looks to be indexed to its exact station list. Logged rather than chased; the two fixes above came out of the attempt. | `logged` |
| 14 | **Ink — buildings had no lines but their silhouette** | Found by turning the ink bright red and photographing: a cottage 16 m away had a line round its roof against the sky and NOTHING else - not round its window frames, not down its timber framing, not at the corner where two walls meet. `ink` is a DEPTH-break pass, and those are centimetres of depth at that range, under the 2% floor. So every building was a flat shape with a coloured pattern on it, which is a large part of why the game reads as generated rather than drawn. A corner is an ANGLE difference, not a depth one: the pass now rebuilds the surface normal from the depth buffer twice - once from the neighbours ahead, once from behind, which agree exactly on any smooth surface - and inks where they part. Scale-free, so one threshold separates the terrain's few-degree facets from a building's right angle. Measured free: 8.5 ms median with it off, 8.1 with it on. `qc_inkred.png` (the diagnosis), `qc_crease2.png`, `qc_crease_city.png`. | `fixed` |
| 16 | Ink — grazing artefact | The corner term drew faint scratches across the village green: a normal rebuilt from depth is only as good as the depth is smooth across a pixel, and ground seen at a glancing angle spans a lot of it per pixel, so the two normals disagreed on flat grass. Faded out as the surface approaches edge-on, which costs corners nothing - a corner worth drawing is one you are looking AT. `qc_village_eye_final.png` (scratches) vs `qc_grazing.png` (clean). | `fixed` |
| 3c | ↳ village lane, SHIPPED | The ground's own colour now arrives halfway down the skirt instead of being stretched its whole width, so a 4 m lane reads as a 4 m lane. Geometry untouched - the skirt still eases over its full 5.4 m, so no height, guard or collision changes at all. This is the fix that needed the lane topology to be derived first (#15); the two earlier attempts failed because it was hard-coded. `qc_village_final.png`, `qc_village_eye_final.png`. | `fixed` |
| 15 | Ribbon lane topology | `SECTION_LANES = 19` and `splits_at(lane) = 4 \| 6 \| 11 \| 13` both described what `cross_section` emits, and neither was derived from it - so adding one colour-only station put 711 of a village's 27,033 paving triangles face down: the stride stayed 19 while the row became 21, splicing lanes from one cross-section onto the next, and all four split indices had moved by one. Diagnosed by Codex reading the emission against the constants. A `Lane` now carries `splits_after`, set where the duplication happens, and the mesher strides by what the row actually emitted. | `fixed` |
| 20 | Cities — read as a road network | Every city building was `tower()` at a different height: block, tower and spire are one function with 5, 9 and 14 floors. And `HOUSES_IN_A_CITY` was 96 across 36 hectares, so a block held one building and the rest was grass. Four new figures with genuinely different silhouettes - housing slab (long bar with balcony rhythm), retail parade (one long canopy), depot shed (shallow roof, roller doors), car deck (open floors, stripes) - and the districts now build what they are FOR rather than rolling for a height: towers and shopfronts downtown, offices/depot/deck in the working quarter, slabs at the edge. 142 buildings -> 588. `qc_grid_city.png` before, `qc_city_dense.png` after. | `fixed` |
| 19 | **`settle::CELL` is an index that changes its answer** | 512 m cells against a 340 m city, so every building files into one cell and every height sample walks all of them: paving that city went 439 -> 1,719 ms at the same vertex count when the building count rose. Setting it to 64 m gives **281 ms** - a 6x - but breaks two guards (a CityBlock on 0.30 m of fall, and 158 desert cells on the home continent), which means cell size is changing ANSWERS and not just speed. That should be impossible when `index()` files by reach. Not shipped; handed to Codex with the numbers. | `logged` |
| 18 | Cities — cost of density | The denser cities cost real pool time: the ground's 95th wait goes 274 -> 712 ms and one frame in a flight passes 100 ms. Shipped anyway because it is off-frame and the town cap holds it to one city, and because #19 recovers all of it and more once fixed. Honest tradeoff, not a free win. | `logged` |
| 17 | Kerb lines too heavy | Two mechanisms drew one kerb - the authored line (added when `ink` could not see a kerb) and the new corner term (a kerb face is a right angle) - so every kerb was a thick black band. Reported with four photographs. Authored line lightened rather than removed: the pass needs a few pixels of depth, which it has near and not at a hundred metres, while the authored line holds a constant width at any range. | `fixed` |
| 26 | **Direction — a city humans and Copaimo share** | User: more parks and open areas, groomers, shops; ordinary people keep Copaimo, not only Wardens; the Guild owns training, breeding and registry. Both of us researching Pokémon-game cities for the *grammar* (not the plans): Codex's `POKEMON_CITY_DESIGN_INSPIRATION_FOR_COPAIMO_2026-09-02.md` proposes an institution roster (clinic, washhouse/groomer, provisions, wayfarers' court, commons, sanctuary at the edge, board/mount exchange) and - the structural answer to "too crowded" - open space reserved as programmed rooms BEFORE lots are filled, 12-30% of city area by character. My brief follows. | `logged` - active design |
| 25 | Cities — density | 420 read as "slightly too crowded" (user). Now 280: at 300 one CityTower lands on the pad seam (#22) and fails the uneven-ground guard by 5 cm; at 280 that lot gets a different neighbour. That is the roster count moving a seed-fragile fault around, not curing it, and the next roster change may move it back. Documented at the constant. | `fixed` (fragile - see #22) |
| 24 | Cities — the car deck | `city_deck` was authored as a car park with a 13-degree vehicle ramp in a world with **no cars** - Codex read it against the fiction and called it a P1 coherence fault. Rebuilt as a pedestrian exchange with the same striped silhouette: ramp replaced by a public stair (Codex measured my first cut at a 39-degree, 243 mm-rise ladder; now 20 rises at 170 mm with a landing and two rails), continuous guards on the terrace, banners on rods, an arcade of stalls at ground. Follow-ups from Codex's review, not done: upper decks are still empty with no visible circulation past the first; the rear ground faces are blank boxes; prove ground + first terrace as a place before extending. `qc_deck2.png`. | `fixed` (programme) / `logged` (occupation) |
| 23 | Cities — spire in a road | `lot_that_fits` swaps the spire onto an existing lot with 5 m of slack and no street check, so a spire could overhang a lot sized for a smaller building - a kerb corner landed exactly 0.0 m inside its wall. The other spire seat already asked `clear_of_streets`; this one does now too, and `off_this_street` additionally tests the street's own kerb CORNERS against the footprint, which the centre-to-nearest-point measure missed at a street's end. Two derivations of "off the street" reconciled. | `fixed` |
| 22 | **Pads — the seam** | Two buildings' pads with different levels overlap on a slope; `pad_under` blends them by inverse distance, and the 2 m terrain grid's vertices sit outside the footprint where the neighbour's share is comparable, so the DRAWN ground under a corner is off. Measured at one lot: dry ground falls 0.59 m across the footprint, settled ground 2.2 m, pad target flat at 25.0. The guard's own note says the honest fix is a denser mesh under settlements. Consequence tonight: `CityBlockLow`/`CityBlockTall` are built and wired but NOT dealt - dealing them reshuffles neighbours onto the seam. Held until the seam is fixed, per Codex's advice. | `logged` |
| 21 | **Index — one constant, two meanings** | `settle::CELL` (512 m) sized the spatial index AND was the search radius in `approach()`. Shrinking the cell to 64 m for a 6x paving win turned every town (roads 100 m off stopped counting toward its bearing) and moved the world - a CityBlock onto a slope and 158 desert cells onto the home continent. Codex found the one line that read `CELL` as a distance. Split: `APPROACH_WITHIN = 512` keeps the bearing, `CELL = 64` indexes. Both guards pass; worst city paving 1,719 -> 297 ms with 4x the buildings. | `fixed` |
